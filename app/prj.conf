# quickstart-bluetooth application configuration.
#
# PLACEHOLDER (NPE-1770 scaffolding). The full configuration — BLE + LBS, MDS
# gateway path, Memfault core, settings/serial provisioning, device ID, DIS —
# is added in NPE-1772 per PLAN.md §4.3.

CONFIG_LOG=y
